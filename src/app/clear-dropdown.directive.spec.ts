import { ClearDropdownDirective } from './clear-dropdown.directive';

describe('ClearDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
